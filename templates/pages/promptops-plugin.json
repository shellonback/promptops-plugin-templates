{
  "schemaVersion": 1,
  "id": "your-handle.release-notes",
  "name": "Release Notes",
  "version": "0.1.0",
  "category": "pages",
  "description": "Adds a page that turns the recent commits of a repository into release notes and saves them in docs/.",
  "minAppVersion": "2.1.0",
  "entry": "dist/plugin.js",
  "permissions": ["git:read", "ai:generate", "workspace:write"],
  "providers": ["claude-code-cli"],
  "contributes": {
    "pages": [{ "id": "release-notes", "title": "Release Notes", "icon": "ti-notes" }]
  }
}
