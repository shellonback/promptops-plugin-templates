// AGENT template: this section is declarative. Everything is in promptops-plugin.json
// and in the files it points to. This bundle stays empty on purpose: `entry` is
// required by the manifest, and an empty bundle asks for no permission at all.
